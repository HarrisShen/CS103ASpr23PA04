
<h1> Transactions for <%= user.username %> </h1>

<p> Here is your transactions, grouped by categories... </p>

<ul>
  <li> <a href="/transaction"> back to all transactions </a> </li> 
</ul>


<table class="table table-striped table-hover table-bordered">
  <thead>
    <th> Category </th>
    <th> Total amount </th>
  </thead>
  <% items.forEach(item => { %>
    <tr>
      <td> <%= item._id %> </td>
      <td> <%= item.total %> </td>
    </tr>
  <% }) %>
</table>